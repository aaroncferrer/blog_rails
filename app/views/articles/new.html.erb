<section class="new">
    <h1 class="form_title">Create New Article</h1>

    <%= render "form" %>

    <!--go back-->
    <p> 
        <% if @article.errors.any? %>
            <% @article.errors.full_messages.each do |message| %>
                <p><%= message %></p>
            <% end %>
        <% end %>
    </p>
</section>

